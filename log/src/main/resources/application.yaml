spring:
  application:
    name: log

  rabbitmq:
    host: localhost
    port: 5672
    username: username
    password: password

server:
  port: 9002

app:
  config:
    name: hello
    consumer-information-list:
      - queue-name: example-queue
        db_source: elk-source
